# OneZero's Prettier Config

### How to use

Install this package as a developer dependency:

`npm install --save-dev @onezerocompany/prettier-config`

###### Package.json

```JSON
{
  "prettier": "@onezerocompany/prettier-config"
}
```
